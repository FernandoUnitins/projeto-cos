info:
  name: Criar Tarefa
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8082/tarefas
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
